# Hysteria QUIC UDP buffer tuning.
# quic-go expects at least ~7.5 MiB; 16 MiB is the value recommended by the
# upstream Hysteria docs to avoid "failed to sufficiently increase receive
# buffer size" warnings under load.
net.core.rmem_max = 16777216
net.core.wmem_max = 16777216
